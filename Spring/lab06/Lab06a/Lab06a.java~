import java.util.Scanner;

/**
 * The program convert take input from user and convert it two binary base 
 * @author Ahmet Faruk Ulutaþ
 * @version 02/04/2020
 */ 
public class Lab06a
{
   static int toBinary( String decimal) { // the corresponding binary String and returns it. 
      int temp, result;
      
      temp = decimal % 10;
      if  
   }
   
   static int toDecimal( String binary) { //  takes a ?String ?base2 and converts it into its decimal
         // --base10-- equivalent, as an int and returns it. For example, if base2 is "110", it should return 6, 
         // whereas if it was "1011", it should return 11. ?You should be able to do this without using Math class
         // methods, and without computing the power of the most significant digit. 
      int temp, result;
      
      temp = decimal % 10;
      if  
   }
   
   public static void main( String[] args)
   {
      Scanner scan = new Scanner( System.in);
      
      // variables
      int n;
      
      // program code
      
      // ask prompt and initilase
      System.out.print( "Enter an integer n: ");
      n = scan.nextInt();
      
      // print binary and decimal representation of n by using methods
      System.out.println( "The binary representation is " + toBinary( n) );
      System.out.println( "The decimal value is " + toDecimal( n) );
      
      // if it is same number, print they are equal
      if ( toDecimal( n).equals( toBinary( n) ) ) {
         System.out.println( "They are equal." );
      }
   }

}